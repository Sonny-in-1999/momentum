const me = true;
const you = false;

const mom = null; //공간차지 값이 존재함(empty)
let dad; //let dad = undefined; 공간차지 값이 없음

document.write(me);
document.write(you);
document.write(mom);
document.write(dad);